const request = require('supertest');
const app = require('../app');

process.env.NODE_ENV = 'test';

describe('Unit tests', () => {
    it('Hashing string', () => {});
    it('Generate salt', () => {});
    it('Generate token', () => {});
});